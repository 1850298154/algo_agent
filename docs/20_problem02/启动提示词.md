请根据我提供给你的数据环境（包括无人机需要对应执行任务的所有目标），优化最佳分配方案，需要所有任务都完成的时间最优化，自行分析问题建立数学建模，查阅资料（搜索deepwiki该问题所有可以利用的解决方法，要尝试所有的方法进行对比择优，如果不会求解或者写代码可以问deepwiki如何求解），设计方案（记录实验数据、统计维度和指标），并用python解决得到最终计算结果，并告诉我你的指标和其他各种方案的代码（你自己按照其他人思路实现）对比结果如何？

对每一个算法都需要：统计各个维度进行画图对比，不限于如下指标：
0. 所有的uav在时间轴上的甘特图（被分配的任务的执行时间段）以及无人机的状态（去往任务和目标的导航/等待悬停/执行任务目标进行中）在时间轴上的变化图，不可能没有状态。
1. 各个任务响应时间（从任务被创建到执行结束到首次被执行的时间）[s]
2. 所有任务平均响应时间（从任务被创建到执行结束到首次被执行的时间）[s]
3. 各个任务的完成时间（从任务被创建到执行结束）[s]
4. 任务的平均完成时间（从任务被创建到执行结束）[s]
5. 所有任务完成的总耗时[s]
6. 各个时刻uav处于路径导航旅行（朝向下一个任务飞行的路途）/等待（未被分配任务）/正在执行执行任务的机器人数uav数及占比
7. 最终任务结束后，所有无人机的导航/等待/执行，的时间的累计以及占比（扇形图）
8. 任务的成功率[%]
9. 算法的各种超参数对计算时间的影响
10. 算法的各种超参数对计算空间的影响
以上指标仅供参考，不限制于此，欢迎你根据你的分析设计出其他有意义的指标进行对比分析。如果没有办法统计，不要编造，但是要尽可能的挖掘出有意义的指标进行对比分析。
