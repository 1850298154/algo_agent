
你所在的工作路径下面，可以用python工具  ExecutePythonCodeTool  写一段包含读取json文件的代码，读取一下文件
例如：“
import json
with open('xx_schema.json', 'r', encoding='utf-8') as file:
    schema = json.load(file)
    print(json.dumps(schema, indent=2))
”，读取以下文件：
1. **['metro-draw-schema.json', 'route_schema.json']** - 完整的数据结构Schema定义
2. **['metro-draw-data-80%.json', 'route_data.json']** 场景数据。
3. 不要输出文件内容，因为数量会很大，只需按照schema.json中定义的字段读取部分内容进行分析。


你的起点经纬度是：[116.39088849999999, 39.92767]，你需要完整route_data.json文件中的所有路由起点和终点送货，类似旅行商遍历完所有路由，不过需要先拿货，在送货。要尽可能的乘坐地铁，可以节约能量。实在无法乘坐地铁则 需要步行。必须详细的输出完整的路径信息，你可以尝试任何算法，尝试不同的方法。


1. 必须使用python工具进行算法编码输出得到计算答案，不能直接给出答案。
2. 在每一次读取或者计算结束，将数据画图存下来起一个带编号的有意义的名字，
3. 并将当前工作路径和图片名字拼接起来输出图片的绝对路径输出出来。
4. 如果存储文件也要使用utf-8编码存储。




一共给你5个agent，分别是：agent_1, agent_2, agent_3, agent_4, agent_5。所有agent的起点都是[116.39088849999999, 39.92767]，你需要完成route_data.json文件中的所有路由起点和终点送货，类似旅行商遍历完所有路由，不过需要先拿货，在送货。要尽可能的乘坐地铁，可以节约能量。实在无法乘坐地铁则需要步行。必须详细的输出完整的路径信息，你可以尝试任何算法，尝试不同的方法。


- **具体问题陈述**：展示了LLM生成地铁-机器人协同运输路线，但**未与传统的MIP求解器（如Gurobi、CPLEX）或启发式调度算法进行定量对比**。无法证明"LLM驱动"相较于"经典优化方法"的优越性。
- **如何补救**：增加一组对比实验，用Gurobi求解相同的MIP模型，对比：（1）求解时间；（2）解的质量（Makespan）；（3）可行性。若LLM在求解速度或处理大规模问题时有优势，需给出具体数据支撑。



请根据我提供给你的数据环境（包括agent需要对应不同任务的不同目标），优化最佳分配方案，需要所有任务都完成的时间最优化，自行设计方案（记录实验数据、统计维度和指标），并用python解决得到最终计算结果，并告诉我你的指标和其他各种方案的代码（你自己按照其他人思路实现）对比结果如何？

对每一个算法都需要：统计各个维度进行画图对比，不限于如下指标：
1. 所有的agent在时间轴上的甘特图（被分配的任务的执行时间段）以及agent的状态（去往任务目标时候坐地铁、走路，拿到货物坐地铁、走路）在时间轴上的变化图，不可能没有状态。
   1. 甘特图中表示每个agent都要出现
   2. 甘特图中四种不同状态使用不同颜色
   3. 纵轴标注agent编号，横轴标注时间，甘特图标注清楚每个任务编号目标（拿货为开始/送货到尾为结束）
   4. 甘特图上下之间的间距不要，然后它的甘特图的高度也要尽量小（只要能放下文字就行）
2. 各个任务响应时间（从任务被创建到执行结束到首次被执行的时间）[s]
3. 所有任务平均响应时间（从任务被创建到执行结束到首次被执行的时间）[s]
4. 各个任务的完成时间（从任务被创建到执行结束）[s]
5. 任务的平均完成时间（从任务被创建到执行结束）[s]
6. 所有任务完成的总耗时[s]
7. 各个时刻agent处于路径导航旅行（朝向下一个任务飞行的路途）/等待（未被分配任务）/正在执行执行任务的机器人数agent数及占比
8. 最终任务结束后，所有agent的导航/等待/执行，的时间的累计以及占比（扇形图）
9. 任务的成功率[%]
10. 算法的各种超参数对计算时间的影响
11. 算法的各种超参数对计算空间的影响
12. 最终轨迹图，要使用地图底图，标注出每个agent的路径轨迹，起点和终点位置，以及任务编号和目标编号，每个agent运行的是彩虹渐变色路径。可以参考并包含 ref_plot.py 的所有的所有的画图设计。
以上指标仅供参考，不限制于此，欢迎你根据你的分析设计出其他有意义的指标进行对比分析。如果没有办法统计，不要编造，但是要尽可能的挖掘出有意义的指标进行对比分析。
记得把数据存下来，中间计算过程也存到一个子文件夹下，画图也存到拎一个子文件夹下。不同过程看着安排子文件件（可以带个序号+名字的文件夹）。

