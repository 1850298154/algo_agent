[豆包](https://www.doubao.com/chat/38416008670589442)


# 任务规划与多智能体系统 RAG 效能自动化评测指令 (2026年2-3月 Python 库真实Feature基准测试)

你现在是一个严谨的 AI 评测系统核心引擎。你需要执行一项针对“任务规划（Task Planning）与多智能体（Multi-Agent）”领域的 A/B 测试。
本次测试将严格比对你在**“仅依赖预训练内部知识（Baseline）”**与**“调用外部搜索工具（RAG）”**两种状态下，对 **2026年2-3月GitHub/PyPI最新发布的Python库Feature** 的代码生成与执行能力。

## 评测执行规则（严格遵守）

对于下方的 10 个测试题，你必须**逐题**按以下三个步骤输出你的执行过程，严禁跳步：

* **步骤一 `[Baseline - 无检索]`**：**绝对禁止**调用任何工具。仅根据你的内部知识（截止2025年10月），直接给出安装命令、代码方案。
* **步骤二 `[RAG - 调用搜索]`**：使用 `general_search` 工具搜索问题中涉及的 **2026年2-3月最新Python库版本、API变更、安装方式**。根据搜索到的真实上下文，给出修正后的准确安装命令（必须使用 `uv pip install`）和可执行代码。
* **步骤三 `[评判与打分]`**：作为判别器，对比两次作答。基于真实搜索结果，对 Baseline 和 RAG 方案分别打分（满分 100 分）。必须在输出结尾显式输出格式为：`Baseline_Score: [分数]`, `RAG_Score: [分数]`。打分标准如下：
    * *安装正确性 (30分)*：是否使用 `uv` 且安装了正确的2026年2-3月最新版本库。
    * *API/代码准确率 (40分)*：API名称、参数、调用方式是否与2026年2-3月的真实库完全一致。
    * *可执行性 (30分)*：代码是否能在假定的Python 3.10+环境中顺利执行。

---

## 测试数据集（共 10 题）

### 场景 A：2026年2-3月最新多智能体框架与API（4题）
1. 使用 **2026年3月发布的 LangGraph 1.0 正式版** 的 **Functional API**，构建一个包含“Planner”和“Executor”节点的简单任务流。要求：使用 `uv` 安装正确版本，代码必须体现 1.0 版本的新特性（废弃类式构建，使用函数式定义节点和边）。
2. 基于 **2026年2月OpenAI开源的 Swarm 多智能体框架**，编写一段代码：定义两个Agent（“客服”和“技术支持”），实现用户输入“退款”时自动转接给“客服”，输入“代码报错”时自动转接给“技术支持”。要求：使用 `uv` 安装 Swarm，代码使用官方最新的 `Swarm` 类和 `hand_off` 机制。
3. 调用 **2026年2月27日发布的 `langgraph-api 0.7.58`**，启动一个本地LangGraph Server并创建一个带持久化状态的Thread。要求：使用 `uv` 安装 `langgraph-cli[inmem]`，代码包含正确的Server启动命令和Thread创建API调用。
4. 使用 **2026年2月GitHub趋势榜的 `nanobot` 轻量级Agent框架**（Python版），编写一个“文件整理助手”Agent：能识别当前目录下的 `.pdf`、`.docx` 文件并移动到对应文件夹。要求：使用 `uv` 安装 `nanobot`，代码使用其最新的Skill定义方式。

### 场景 B：2026年2-3月运筹优化与路径规划库新Feature（3题）
5. 利用 **2026年1月16日发布的 `pathfinding 1.0.20` 库** 中的 **Iterative Deeping A* (IDA*) 算法**，解决一个5x5的2D迷宫路径规划问题。要求：使用 `uv` 安装 `pathfinding`，代码正确调用 `IDAStarFinder` 类。
6. 基于 **2026年2月更新的 Google OR-Tools（支持Python 3.14）**，编写一个简单的车辆路径问题（VRP）代码：3辆车，5个配送点，最小化总行驶距离。要求：使用 `uv` 安装 `ortools`，代码使用OR-Tools最新的 `pywrapcp` 或 `routing_enums_pb2` 接口。
7. 调用 **2026年2月GitHub更新的 `roster-wizard` 库**（基于OR-Tools），编写一个简单的员工排班代码：2名员工，一周7天，每天需要1人值班，每人每周至少休息2天。要求：使用 `uv` 安装 `roster-wizard`（或其依赖的最新版OR-Tools），代码使用库的最新排班API。

### 场景 C：2026年2-3月开发工具与自动化流程（3题）
8. 遵循 **2026年2月GitHub发布的 Agentic Workflows 最佳实践**，使用 `uv` 安装 `github-agentic` 库（假设名称），编写一段代码：自动为仓库的新Issue添加“bug”或“feature”标签，并分配给指定维护者。要求：代码使用库的最新Webhook事件处理API。
9. 利用 **2026年2月更新的 Docker SDK for Python（支持Rootless模式）**，编写代码：在Rootless模式下启动一个Nginx容器，并配置eBPF网络过滤规则。要求：使用 `uv` 安装 `docker`，代码使用最新的 `DockerClient` 配置Rootless参数。
10. 使用 **2026年3月发布的 `openai 2.26.0` 库**，编写一段异步代码：调用GPT-4o模型处理一张本地图片，识别图片中的物体并返回JSON格式结果。要求：使用 `uv` 安装 `openai[aiohttp]`，代码使用最新的异步 `AsyncOpenAI` 类和Vision API。

---

## 最终报告与统计（非常重要）

在完成上述 10 道题的循环作答与打分后，你必须执行严格的**统计与报告生成**。禁止伪造数据，必须提取你刚刚生成的 20 个分数（每题 2 个）进行求平均值计算。

**输出要求：**
1. **聚合数据表**：
    列出场景 A、场景 B、场景 C 的平均 Baseline 分数和平均 RAG 分数。格式如下：
    | 场景 | 平均 Baseline 分数 | 平均 RAG 分数 |
    |------|---------------------|----------------|
    | A    | [计算值]            | [计算值]       |
    | B    | [计算值]            | [计算值]       |
    | C    | [计算值]            | [计算值]       |
2. **差异归因分析**：
    基于真实执行的情况，总结大型语言模型在缺乏 2026年2-3月最新外部搜索知识时，最常犯的 3 类错误（如：API版本过时、安装方式错误、新特性语法不兼容）。
3. **可视化图表生成（Python 可执行代码）**：
    生成一段自带你刚才计算出的**真实平均分数**作为数据源的 Python 代码（使用 `matplotlib`）。代码要求：
    - 绘制分组柱状图 (Grouped Bar Chart)。
    - X轴分为三组：场景 A (多智能体框架)、场景 B (运筹优化)、场景 C (开发工具)。
    - Y轴为平均得分 (0-100)。
    - 图例清晰对比 "Baseline (无检索)" 与 "RAG (有检索)"。
    - 添加数据标签，显示具体数值。
    - 确保代码可直接复制运行，且不出现“deepwiki”字样。